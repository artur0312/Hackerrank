#include<iostream>
#include<algorithm>
#include<string>

using namespace std;

int main(){
  int n;
  cin>>n;
  string s=to_string(n);
  int digits=0;
  for(int i=0;i<s.size();i++){
    int d=s[i]-'0';
    if(d!=0 && n%d==0){
      digits++;
    }
  }
  cout<<digits;
}
